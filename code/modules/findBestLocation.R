find_best_location <- function(problem.data){
  #Inputs:
  #problem.data: list constructed from make_problem_data()
  #Output: 
  #Save optimal factory configuration to '../variables/output.rds'
  
  attach(problem.data)
  
  ###Your code
  
  detach(problem.data)
}